extends layout

block content
    div(class="content")
        h1 #{title}
        if items.length
            each item in items 
                div= item.name
            div you can not remove a category which has items.
        else
            form(method='POST' action='')
                div.form-group
                    div are you sure you want to delete this category?
                button.btn.btn-primary(type='submit') Submit