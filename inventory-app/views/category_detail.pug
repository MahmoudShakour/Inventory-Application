extends layout 

block content 
    title= title
    div(class="content")
        div= category.name
        div= "description: "+category.description
        each item in item_list
                li 
                    a(href=item.url)= item.name 
        else 
            li
                there is no categories in here!