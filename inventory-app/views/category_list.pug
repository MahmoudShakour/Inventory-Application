extends layout 

block content 
    title= title
    div(class="content")
        div categories
        ul
        each category in category_list
            li 
                a(href=category.url)= category.name 
        
        else 
            li there is no categories in here!