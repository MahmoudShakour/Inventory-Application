extends layout 

block content 
    div(class="content")
        h1= title 
        ul
            each item in item_list   
                li 
                    a(href=item.url)= item.name 
            else 
                li there is no items in there
